// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Defines a synthetic label whose value is derived from existing metric labels or trace span tags via mapping or constructed-value rules. The derived label can then be referenced in queries as if it were a real label on the source series.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as chronosphere from "@pulumi-chronosphere/pulumi-chronosphere";
 *
 * const tier = new chronosphere.DerivedLabel("tier", {
 *     description: "Derives a 'tier' label (read/write/admin) from the instance label",
 *     labelName: "tier",
 *     metricLabel: {
 *         constructedLabel: {
 *             valueDefinitions: [
 *                 {
 *                     filters: [{
 *                         name: "instance",
 *                         valueGlob: "reader-*",
 *                     }],
 *                     value: "read",
 *                 },
 *                 {
 *                     filters: [{
 *                         name: "instance",
 *                         valueGlob: "writer-*",
 *                     }],
 *                     value: "write",
 *                 },
 *                 {
 *                     filters: [{
 *                         name: "instance",
 *                         valueGlob: "admin-*",
 *                     }],
 *                     value: "admin",
 *                 },
 *             ],
 *         },
 *     },
 *     name: "Tier from instance name",
 *     slug: "tier-from-instance",
 * });
 * ```
 */
export class DerivedLabel extends pulumi.CustomResource {
    /**
     * Get an existing DerivedLabel resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: DerivedLabelState, opts?: pulumi.CustomResourceOptions): DerivedLabel {
        return new DerivedLabel(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'chronosphere:index/derivedLabel:DerivedLabel';

    /**
     * Returns true if the given object is an instance of DerivedLabel.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is DerivedLabel {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === DerivedLabel.__pulumiType;
    }

    /**
     * Free-form description of the derived label.
     */
    declare public readonly description: pulumi.Output<string | undefined>;
    /**
     * Policy controlling behavior when the target label already exists on the source series (e.g. keep, replace).
     */
    declare public readonly existingLabelPolicy: pulumi.Output<string | undefined>;
    /**
     * Name of the label exposed on derived series. Must be unique across the system.
     */
    declare public readonly labelName: pulumi.Output<string>;
    /**
     * Derives a label for metrics, either by constructing a new value from filters or by mapping an existing label. Mutually exclusive with `spanTag`.
     */
    declare public readonly metricLabel: pulumi.Output<outputs.DerivedLabelMetricLabel | undefined>;
    /**
     * Display name of the derived label. Can be changed after creation.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Stable identifier for the derived label. Generated from `name` if omitted. Immutable after creation.
     */
    declare public readonly slug: pulumi.Output<string>;
    /**
     * Derives a label for trace spans by mapping from an existing span tag. Mutually exclusive with `metricLabel`.
     */
    declare public readonly spanTag: pulumi.Output<outputs.DerivedLabelSpanTag | undefined>;

    /**
     * Create a DerivedLabel resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: DerivedLabelArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: DerivedLabelArgs | DerivedLabelState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as DerivedLabelState | undefined;
            resourceInputs["description"] = state?.description;
            resourceInputs["existingLabelPolicy"] = state?.existingLabelPolicy;
            resourceInputs["labelName"] = state?.labelName;
            resourceInputs["metricLabel"] = state?.metricLabel;
            resourceInputs["name"] = state?.name;
            resourceInputs["slug"] = state?.slug;
            resourceInputs["spanTag"] = state?.spanTag;
        } else {
            const args = argsOrState as DerivedLabelArgs | undefined;
            if (args?.labelName === undefined && !opts.urn) {
                throw new Error("Missing required property 'labelName'");
            }
            if (args?.name === undefined && !opts.urn) {
                throw new Error("Missing required property 'name'");
            }
            resourceInputs["description"] = args?.description;
            resourceInputs["existingLabelPolicy"] = args?.existingLabelPolicy;
            resourceInputs["labelName"] = args?.labelName;
            resourceInputs["metricLabel"] = args?.metricLabel;
            resourceInputs["name"] = args?.name;
            resourceInputs["slug"] = args?.slug;
            resourceInputs["spanTag"] = args?.spanTag;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(DerivedLabel.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering DerivedLabel resources.
 */
export interface DerivedLabelState {
    /**
     * Free-form description of the derived label.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * Policy controlling behavior when the target label already exists on the source series (e.g. keep, replace).
     */
    existingLabelPolicy?: pulumi.Input<string | undefined>;
    /**
     * Name of the label exposed on derived series. Must be unique across the system.
     */
    labelName?: pulumi.Input<string | undefined>;
    /**
     * Derives a label for metrics, either by constructing a new value from filters or by mapping an existing label. Mutually exclusive with `spanTag`.
     */
    metricLabel?: pulumi.Input<inputs.DerivedLabelMetricLabel | undefined>;
    /**
     * Display name of the derived label. Can be changed after creation.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Stable identifier for the derived label. Generated from `name` if omitted. Immutable after creation.
     */
    slug?: pulumi.Input<string | undefined>;
    /**
     * Derives a label for trace spans by mapping from an existing span tag. Mutually exclusive with `metricLabel`.
     */
    spanTag?: pulumi.Input<inputs.DerivedLabelSpanTag | undefined>;
}

/**
 * The set of arguments for constructing a DerivedLabel resource.
 */
export interface DerivedLabelArgs {
    /**
     * Free-form description of the derived label.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * Policy controlling behavior when the target label already exists on the source series (e.g. keep, replace).
     */
    existingLabelPolicy?: pulumi.Input<string | undefined>;
    /**
     * Name of the label exposed on derived series. Must be unique across the system.
     */
    labelName: pulumi.Input<string>;
    /**
     * Derives a label for metrics, either by constructing a new value from filters or by mapping an existing label. Mutually exclusive with `spanTag`.
     */
    metricLabel?: pulumi.Input<inputs.DerivedLabelMetricLabel | undefined>;
    /**
     * Display name of the derived label. Can be changed after creation.
     */
    name: pulumi.Input<string>;
    /**
     * Stable identifier for the derived label. Generated from `name` if omitted. Immutable after creation.
     */
    slug?: pulumi.Input<string | undefined>;
    /**
     * Derives a label for trace spans by mapping from an existing span tag. Mutually exclusive with `metricLabel`.
     */
    spanTag?: pulumi.Input<inputs.DerivedLabelSpanTag | undefined>;
}
