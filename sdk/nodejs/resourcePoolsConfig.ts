// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Singleton allocation of metric license capacity into named pools that match metrics by label filter, each with priority-based dropping when the pool's allocation is exceeded.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as chronosphere from "@pulumi-chronosphere/pulumi-chronosphere";
 *
 * const _default = new chronosphere.ResourcePoolsConfig("default", {
 *     defaultPool: {
 *         priorities: {
 *             highPriorityMatchRules: ["cluster:production*"],
 *             lowPriorityMatchRules: ["cluster:test*"],
 *         },
 *     },
 *     pools: [{
 *         allocation: {
 *             fixedValue: [{
 *                 license: "PERSISTED_WRITES_STANDARD",
 *                 value: 1000,
 *             }],
 *             percentOfLicense: 49.9,
 *         },
 *         matchRules: [
 *             "team:platform",
 *             "env:production",
 *         ],
 *         name: "first",
 *         priorities: {
 *             highPriorityMatchRules: ["cluster:production*"],
 *             lowPriorityMatchRules: ["cluster:test*"],
 *         },
 *     }],
 * });
 * ```
 */
export class ResourcePoolsConfig extends pulumi.CustomResource {
    /**
     * Get an existing ResourcePoolsConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ResourcePoolsConfigState, opts?: pulumi.CustomResourceOptions): ResourcePoolsConfig {
        return new ResourcePoolsConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'chronosphere:index/resourcePoolsConfig:ResourcePoolsConfig';

    /**
     * Returns true if the given object is an instance of ResourcePoolsConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ResourcePoolsConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ResourcePoolsConfig.__pulumiType;
    }

    /**
     * Catch-all pool that receives metrics not matched by any other pool. Also receives any license allocation left unassigned by the other pools.
     */
    declare public readonly defaultPool: pulumi.Output<outputs.ResourcePoolsConfigDefaultPool | undefined>;
    /**
     * Named pools that partition each license across teams or workloads. Pools are matched in declaration order via their `matchRules`.
     */
    declare public readonly pool: pulumi.Output<outputs.ResourcePoolsConfigPool[] | undefined>;
    /**
     * Deprecated: use `pool` instead. Set of named pools that partition the license.
     *
     * @deprecated Use pool instead of pools
     */
    declare public readonly pools: pulumi.Output<outputs.ResourcePoolsConfigPool[] | undefined>;

    /**
     * Create a ResourcePoolsConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: ResourcePoolsConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ResourcePoolsConfigArgs | ResourcePoolsConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ResourcePoolsConfigState | undefined;
            resourceInputs["defaultPool"] = state?.defaultPool;
            resourceInputs["pool"] = state?.pool;
            resourceInputs["pools"] = state?.pools;
        } else {
            const args = argsOrState as ResourcePoolsConfigArgs | undefined;
            resourceInputs["defaultPool"] = args?.defaultPool;
            resourceInputs["pool"] = args?.pool;
            resourceInputs["pools"] = args?.pools;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ResourcePoolsConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ResourcePoolsConfig resources.
 */
export interface ResourcePoolsConfigState {
    /**
     * Catch-all pool that receives metrics not matched by any other pool. Also receives any license allocation left unassigned by the other pools.
     */
    defaultPool?: pulumi.Input<inputs.ResourcePoolsConfigDefaultPool | undefined>;
    /**
     * Named pools that partition each license across teams or workloads. Pools are matched in declaration order via their `matchRules`.
     */
    pool?: pulumi.Input<pulumi.Input<inputs.ResourcePoolsConfigPool>[] | undefined>;
    /**
     * Deprecated: use `pool` instead. Set of named pools that partition the license.
     *
     * @deprecated Use pool instead of pools
     */
    pools?: pulumi.Input<pulumi.Input<inputs.ResourcePoolsConfigPool>[] | undefined>;
}

/**
 * The set of arguments for constructing a ResourcePoolsConfig resource.
 */
export interface ResourcePoolsConfigArgs {
    /**
     * Catch-all pool that receives metrics not matched by any other pool. Also receives any license allocation left unassigned by the other pools.
     */
    defaultPool?: pulumi.Input<inputs.ResourcePoolsConfigDefaultPool | undefined>;
    /**
     * Named pools that partition each license across teams or workloads. Pools are matched in declaration order via their `matchRules`.
     */
    pool?: pulumi.Input<pulumi.Input<inputs.ResourcePoolsConfigPool>[] | undefined>;
    /**
     * Deprecated: use `pool` instead. Set of named pools that partition the license.
     *
     * @deprecated Use pool instead of pools
     */
    pools?: pulumi.Input<pulumi.Input<inputs.ResourcePoolsConfigPool>[] | undefined>;
}
