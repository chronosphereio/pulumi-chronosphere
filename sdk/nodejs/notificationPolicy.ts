// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Routes alert signals from monitors and SLOs to notifiers, with per-severity routing and label-matcher-based overrides. If `name` is set the policy is independent (referenceable by ID); if `name` is omitted the policy is inline and can only be embedded in another resource (e.g. a bucket).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as chronosphere from "@pulumi-chronosphere/pulumi-chronosphere";
 *
 * const platformTeam = new chronosphere.Team("platformTeam", {name: "Platform"});
 * const email = new chronosphere.EmailAlertNotifier("email", {
 *     name: "Platform Email",
 *     to: "platform@example.com",
 * });
 * const platformNotificationPolicy = new chronosphere.NotificationPolicy("platformNotificationPolicy", {
 *     name: "Platform Policy",
 *     teamId: platformTeam.id,
 *     routes: [{
 *         severity: "warn",
 *         notifiers: [email.id],
 *         groupBy: {
 *             labelNames: ["service"],
 *         },
 *     }],
 * });
 * ```
 */
export class NotificationPolicy extends pulumi.CustomResource {
    /**
     * Get an existing NotificationPolicy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: NotificationPolicyState, opts?: pulumi.CustomResourceOptions): NotificationPolicy {
        return new NotificationPolicy(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'chronosphere:index/notificationPolicy:NotificationPolicy';

    /**
     * Returns true if the given object is an instance of NotificationPolicy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is NotificationPolicy {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === NotificationPolicy.__pulumiType;
    }

    /**
     * Read-only internal marker tracking whether the policy is independent (named) or inline. Used to force replacement when transitioning between the two.
     */
    declare public /*out*/ readonly isIndependent: pulumi.Output<boolean>;
    /**
     * Display name of the notification policy. If unset, the policy is treated as anonymous/inline and cannot be referenced by ID. Changing between set/unset forces resource replacement.
     */
    declare public readonly name: pulumi.Output<string | undefined>;
    /**
     * Computed/optional JSON serialization of the policy. Primarily used to attach inline policy data to other resources (e.g. buckets).
     */
    declare public readonly notificationPolicyData: pulumi.Output<string>;
    /**
     * Ordered overrides that route alerts matching specific label matchers to different destinations. The first matching override is applied; non-matching alerts fall through to the default `route`.
     */
    declare public readonly overrides: pulumi.Output<outputs.NotificationPolicyOverride[] | undefined>;
    /**
     * Per-severity routing rules. Each entry maps a severity (e.g. `warn`, `critical`) to a set of notifiers, destinations, grouping, and repeat behavior.
     */
    declare public readonly routes: pulumi.Output<outputs.NotificationPolicyRoute[] | undefined>;
    /**
     * Stable identifier for the notification policy. Can only be set when `name` is set. Generated from `name` if omitted. Immutable after creation.
     */
    declare public readonly slug: pulumi.Output<string>;
    /**
     * ID of the team that owns this notification policy. Required when `name` is set (anonymous policies cannot be owned).
     */
    declare public readonly teamId: pulumi.Output<string | undefined>;

    /**
     * Create a NotificationPolicy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: NotificationPolicyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: NotificationPolicyArgs | NotificationPolicyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as NotificationPolicyState | undefined;
            resourceInputs["isIndependent"] = state?.isIndependent;
            resourceInputs["name"] = state?.name;
            resourceInputs["notificationPolicyData"] = state?.notificationPolicyData;
            resourceInputs["overrides"] = state?.overrides;
            resourceInputs["routes"] = state?.routes;
            resourceInputs["slug"] = state?.slug;
            resourceInputs["teamId"] = state?.teamId;
        } else {
            const args = argsOrState as NotificationPolicyArgs | undefined;
            resourceInputs["name"] = args?.name;
            resourceInputs["notificationPolicyData"] = args?.notificationPolicyData;
            resourceInputs["overrides"] = args?.overrides;
            resourceInputs["routes"] = args?.routes;
            resourceInputs["slug"] = args?.slug;
            resourceInputs["teamId"] = args?.teamId;
            resourceInputs["isIndependent"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(NotificationPolicy.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering NotificationPolicy resources.
 */
export interface NotificationPolicyState {
    /**
     * Read-only internal marker tracking whether the policy is independent (named) or inline. Used to force replacement when transitioning between the two.
     */
    isIndependent?: pulumi.Input<boolean | undefined>;
    /**
     * Display name of the notification policy. If unset, the policy is treated as anonymous/inline and cannot be referenced by ID. Changing between set/unset forces resource replacement.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Computed/optional JSON serialization of the policy. Primarily used to attach inline policy data to other resources (e.g. buckets).
     */
    notificationPolicyData?: pulumi.Input<string | undefined>;
    /**
     * Ordered overrides that route alerts matching specific label matchers to different destinations. The first matching override is applied; non-matching alerts fall through to the default `route`.
     */
    overrides?: pulumi.Input<pulumi.Input<inputs.NotificationPolicyOverride>[] | undefined>;
    /**
     * Per-severity routing rules. Each entry maps a severity (e.g. `warn`, `critical`) to a set of notifiers, destinations, grouping, and repeat behavior.
     */
    routes?: pulumi.Input<pulumi.Input<inputs.NotificationPolicyRoute>[] | undefined>;
    /**
     * Stable identifier for the notification policy. Can only be set when `name` is set. Generated from `name` if omitted. Immutable after creation.
     */
    slug?: pulumi.Input<string | undefined>;
    /**
     * ID of the team that owns this notification policy. Required when `name` is set (anonymous policies cannot be owned).
     */
    teamId?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a NotificationPolicy resource.
 */
export interface NotificationPolicyArgs {
    /**
     * Display name of the notification policy. If unset, the policy is treated as anonymous/inline and cannot be referenced by ID. Changing between set/unset forces resource replacement.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Computed/optional JSON serialization of the policy. Primarily used to attach inline policy data to other resources (e.g. buckets).
     */
    notificationPolicyData?: pulumi.Input<string | undefined>;
    /**
     * Ordered overrides that route alerts matching specific label matchers to different destinations. The first matching override is applied; non-matching alerts fall through to the default `route`.
     */
    overrides?: pulumi.Input<pulumi.Input<inputs.NotificationPolicyOverride>[] | undefined>;
    /**
     * Per-severity routing rules. Each entry maps a severity (e.g. `warn`, `critical`) to a set of notifiers, destinations, grouping, and repeat behavior.
     */
    routes?: pulumi.Input<pulumi.Input<inputs.NotificationPolicyRoute>[] | undefined>;
    /**
     * Stable identifier for the notification policy. Can only be set when `name` is set. Generated from `name` if omitted. Immutable after creation.
     */
    slug?: pulumi.Input<string | undefined>;
    /**
     * ID of the team that owns this notification policy. Required when `name` is set (anonymous policies cannot be owned).
     */
    teamId?: pulumi.Input<string | undefined>;
}
