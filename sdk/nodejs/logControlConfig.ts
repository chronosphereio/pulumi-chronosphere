// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Singleton org-wide pipeline of rules that sample, drop, replace, parse, or emit metrics from logs at ingest.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as chronosphere from "@pulumi-chronosphere/pulumi-chronosphere";
 *
 * const config = new chronosphere.LogControlConfig("config", {rules: [
 *     {
 *         filter: "service = 'sample-service' AND severity = 'debug'",
 *         mode: "ENABLED",
 *         name: "sample-debug",
 *         sample: {
 *             rate: 0.01,
 *         },
 *         type: "SAMPLE",
 *     },
 *     {
 *         filter: "service = 'deprecated-service'",
 *         mode: "ENABLED",
 *         name: "drop-deprecated",
 *         type: "DROP",
 *     },
 *     {
 *         dropField: {
 *             fieldRegex: "password|secret|api_key",
 *             parentPath: {
 *                 selector: "kubernetes['labels']",
 *             },
 *         },
 *         filter: "service = 'api-gateway'",
 *         mode: "ENABLED",
 *         name: "drop-sensitive-fields",
 *         type: "DROP_FIELD",
 *     },
 *     {
 *         filter: "service = 'api-gateway'",
 *         mode: "ENABLED",
 *         name: "shorten-trace-ids",
 *         replaceField: {
 *             field: {
 *                 selector: "trace_id",
 *             },
 *             replaceAll: false,
 *             replaceMode: "STATIC_VALUE",
 *             replaceRegex: "[0-9a-f]{32}",
 *             staticValue: {
 *                 value: "[trace-id]",
 *             },
 *         },
 *         type: "REPLACE_FIELD",
 *     },
 * ]});
 * ```
 */
export class LogControlConfig extends pulumi.CustomResource {
    /**
     * Get an existing LogControlConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: LogControlConfigState, opts?: pulumi.CustomResourceOptions): LogControlConfig {
        return new LogControlConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'chronosphere:index/logControlConfig:LogControlConfig';

    /**
     * Returns true if the given object is an instance of LogControlConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is LogControlConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === LogControlConfig.__pulumiType;
    }

    /**
     * Ordered list of log control rules applied to the log ingest pipeline. Rules are evaluated in order.
     */
    public readonly rules!: pulumi.Output<outputs.LogControlConfigRule[] | undefined>;

    /**
     * Create a LogControlConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: LogControlConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: LogControlConfigArgs | LogControlConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as LogControlConfigState | undefined;
            resourceInputs["rules"] = state ? state.rules : undefined;
        } else {
            const args = argsOrState as LogControlConfigArgs | undefined;
            resourceInputs["rules"] = args ? args.rules : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(LogControlConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering LogControlConfig resources.
 */
export interface LogControlConfigState {
    /**
     * Ordered list of log control rules applied to the log ingest pipeline. Rules are evaluated in order.
     */
    rules?: pulumi.Input<pulumi.Input<inputs.LogControlConfigRule>[]>;
}

/**
 * The set of arguments for constructing a LogControlConfig resource.
 */
export interface LogControlConfigArgs {
    /**
     * Ordered list of log control rules applied to the log ingest pipeline. Rules are evaluated in order.
     */
    rules?: pulumi.Input<pulumi.Input<inputs.LogControlConfigRule>[]>;
}
