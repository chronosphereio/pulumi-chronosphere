// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Read-only data source that retrieves an existing service by slug. Services are typically discovered automatically from incoming telemetry; use this data source to reference one from Terraform-managed resources.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as chronosphere from "@pulumi-chronosphere/pulumi-chronosphere";
 *
 * const gateway = chronosphere.ServiceDataSource({
 *     slug: "gateway",
 * });
 * const gatewayUp = new chronosphere.Monitor("gatewayUp", {
 *     name: "Gateway up",
 *     collectionId: gateway.then(gateway => gateway.id),
 *     query: {
 *         prometheusExpr: "up{service=\"gateway\"}",
 *     },
 *     seriesConditions: {
 *         conditions: [{
 *             severity: "warn",
 *             value: 1,
 *             op: "LT",
 *         }],
 *     },
 * });
 * ```
 */
export function serviceDataSource(args: ServiceDataSourceArgs, opts?: pulumi.InvokeOptions): Promise<ServiceDataSourceResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("chronosphere:index/serviceDataSource:ServiceDataSource", {
        "slug": args.slug,
    }, opts);
}

/**
 * A collection of arguments for invoking ServiceDataSource.
 */
export interface ServiceDataSourceArgs {
    /**
     * Slug of the service to look up.
     */
    slug: string;
}

/**
 * A collection of values returned by ServiceDataSource.
 */
export interface ServiceDataSourceResult {
    /**
     * Read-only: free-form description of the service.
     */
    readonly description: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * Read-only: display name of the service.
     */
    readonly name: string;
    /**
     * Slug of the service to look up.
     */
    readonly slug: string;
}
/**
 * Read-only data source that retrieves an existing service by slug. Services are typically discovered automatically from incoming telemetry; use this data source to reference one from Terraform-managed resources.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as chronosphere from "@pulumi-chronosphere/pulumi-chronosphere";
 *
 * const gateway = chronosphere.ServiceDataSource({
 *     slug: "gateway",
 * });
 * const gatewayUp = new chronosphere.Monitor("gatewayUp", {
 *     name: "Gateway up",
 *     collectionId: gateway.then(gateway => gateway.id),
 *     query: {
 *         prometheusExpr: "up{service=\"gateway\"}",
 *     },
 *     seriesConditions: {
 *         conditions: [{
 *             severity: "warn",
 *             value: 1,
 *             op: "LT",
 *         }],
 *     },
 * });
 * ```
 */
export function serviceDataSourceOutput(args: ServiceDataSourceOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<ServiceDataSourceResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("chronosphere:index/serviceDataSource:ServiceDataSource", {
        "slug": args.slug,
    }, opts);
}

/**
 * A collection of arguments for invoking ServiceDataSource.
 */
export interface ServiceDataSourceOutputArgs {
    /**
     * Slug of the service to look up.
     */
    slug: pulumi.Input<string>;
}
