# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from .. import _utilities

import types

__config__ = pulumi.Config('chronosphere')


class _ExportableConfig(types.ModuleType):
    @_builtins.property
    def api_token(self) -> Optional[str]:
        """
        API token used to authenticate against the Chronosphere API. Treat as a secret. Falls back to the `CHRONOSPHERE_API_TOKEN` environment variable.
        """
        return __config__.get('apiToken') or _utilities.get_env('CHRONOSPHERE_API_TOKEN')

    @_builtins.property
    def disable_dryrun(self) -> Optional[bool]:
        """
        Disable the dry-run validation step that runs before every apply. Falls back to the `CHRONOSPHERE_DRY_RUN_VALIDATION_DISABLED` environment variable (set to `1` to disable).
        """
        return __config__.get_bool('disableDryrun')

    @_builtins.property
    def entity_namespace(self) -> Optional[str]:
        """
        Optional namespace prefix applied to entity slugs managed by this provider instance, so multiple Terraform configurations can coexist in one Chronosphere org. Falls back to the `CHRONOSPHERE_ENTITY_NAMESPACE` environment variable.
        """
        return __config__.get('entityNamespace')

    @_builtins.property
    def org(self) -> Optional[str]:
        """
        Chronosphere organization name (the subdomain of `<org>.chronosphere.io`). Falls back to the `CHRONOSPHERE_ORG` or `CHRONOSPHERE_ORG_NAME` environment variables.
        """
        return __config__.get('org') or _utilities.get_env('CHRONOSPHERE_ORG', 'CHRONOSPHERE_ORG_NAME')

    @_builtins.property
    def unstable(self) -> Optional[bool]:
        """
        Opt into resources and behaviors backed by Chronosphere's unstable config API. Subject to breaking change without notice. Falls back to the `CHRONOSPHERE_UNSTABLE` environment variable (set to `1` to enable).
        """
        return __config__.get_bool('unstable')

